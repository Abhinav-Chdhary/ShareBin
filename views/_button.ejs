<div class="buttons-wrapper">
    <a href="/" class="logo">ShareBin</a>
    <div class="buttons" type="submit">
        <a href="/new" class="btn">NEW</a>
        <% if(locals.canSave) {%>
        <button class="btn" type="submit">SAVE</button>
        <% } %> <% if (locals.id != null) {%>
        <a href="/<%= locals.id %>/duplicate" class="btn">Duplicate</a>
        <% } %>
    </div>
</div>
